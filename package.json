{
  "name": "Rocket.Chat",
  "scripts": {
    "start": "meteor run",
    "lint": "eslint .",
    "deploy": "npm run build && pm2 startOrRestart pm2.json"
  },
  "devDependencies": {
    "eslint": "^3.1.1"
  }
}
