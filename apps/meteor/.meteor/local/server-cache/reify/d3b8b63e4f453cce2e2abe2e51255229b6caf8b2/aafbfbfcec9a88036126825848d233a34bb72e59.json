"use strict";module.export({isGroupsMessagesProps:()=>isGroupsMessagesProps},true);var Ajv;module.link('ajv',{default(v){Ajv=v}},0);var withGroupBaseProperties;module.link('./BaseProps',{withGroupBaseProperties(v){withGroupBaseProperties=v}},1);

const ajv = new Ajv({
    coerceTypes: true,
});
const GroupsMessagesPropsSchema = withGroupBaseProperties({
    roomId: {
        type: 'string',
    },
    mentionIds: {
        type: 'string',
    },
    starredIds: {
        type: 'string',
    },
    pinned: {
        type: 'string',
    },
    count: {
        type: 'number',
        nullable: true,
    },
    offset: {
        type: 'number',
        nullable: true,
    },
    sort: {
        type: 'string',
        nullable: true,
    },
    query: {
        type: 'string',
        nullable: true,
    },
});
const isGroupsMessagesProps = ajv.compile(GroupsMessagesPropsSchema);
//# sourceMappingURL=GroupsMessagesProps.js.map