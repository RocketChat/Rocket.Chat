{
  "name": "regression",
  "packageManager": "yarn@3.2.2",
  "devDependencies": {
    "@playwright/test": "^1.35.1"
  },
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:unstable": "URL=https://unstable.rocket.chat playwright test",
    "test:unstable:headed": "URL=https://unstable.rocket.chat playwright test --headed"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "monocart-reporter": "^1.7.2"
  }
}
