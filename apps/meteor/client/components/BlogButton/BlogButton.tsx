import React from 'react';
import { Icon } from '@rocket.chat/fuselage';

type Props = {};

const BlogButton = (props: Props) => {
	return <Icon name='image' size='x32' />;
};

export default BlogButton;
