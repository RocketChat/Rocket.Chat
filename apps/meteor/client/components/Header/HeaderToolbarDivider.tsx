import { HeaderV2ToolbarDivider } from '@rocket.chat/ui-client';
import React, { memo } from 'react';

const HeaderToolbarDivider = () => <HeaderV2ToolbarDivider />;

export default memo(HeaderToolbarDivider);
