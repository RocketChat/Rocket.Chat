import React from 'react';
import { Icon } from '@rocket.chat/fuselage';

type Props = {};

const GameButton = (props: Props) => {
	return <Icon name='joystick' size='x32' />;
};

export default GameButton;
