import React from 'react';
import { Icon } from '@rocket.chat/fuselage';

type Props = {};

const HomeButton = ({}: Props) => {
	return <Icon name='home' size='x32' />;
};

export default HomeButton;
