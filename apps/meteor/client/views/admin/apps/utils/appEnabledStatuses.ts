import { AppStatus } from '@rocket.chat/apps-engine/definition/AppStatus';

const appEnabledStatuses = [AppStatus.AUTO_ENABLED, AppStatus.MANUALLY_ENABLED, AppStatus.INITIALIZED];

export default appEnabledStatuses;
