// import { Box } from '@rocket.chat/fuselage';
import { MediaCallRoomSection } from '@rocket.chat/ui-voip';
import { memo } from 'react';

const RoomCallSection = () => {
	return <MediaCallRoomSection />;
};

export default memo(RoomCallSection);
