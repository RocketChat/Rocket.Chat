import React from 'react';
import Page from '../../components/Page';

type Props = {};

const GamesView = (props: Props) => {
	return (
		<Page flexDirection='row'>
			<Page>
				<Page.Content>GamesView</Page.Content>
			</Page>
		</Page>
	);
};

export default GamesView;
