/**
 * This collection serves as buffer for inline results which are retrieved on the client-side
 * @type {Meteor.Collection}
 */
// InlineResultsCache = new Meteor.Collection(null); //todo: provide a cache for client-side retrieved results
