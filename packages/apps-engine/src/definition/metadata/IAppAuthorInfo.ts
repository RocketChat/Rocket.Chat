/** Who wrote the App and where a user goes for help with it. */
export interface IAppAuthorInfo {
	/** The author's name, shown in the administration area. */
	name: string;
	/** The support address users should contact about the App. */
	support: string;
	/** The App's home page. */
	homepage: string;
}
