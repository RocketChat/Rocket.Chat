{
    "extends": ["//"],
    "tasks": {
        "build": {
            "dependsOn": ["^build"],
            "inputs": ["$TURBO_DEFAULT$", "$TURBO_ROOT$/.tool-versions"],
            "outputs": ["definition/**", "lib/**"]
        }
    }
}
