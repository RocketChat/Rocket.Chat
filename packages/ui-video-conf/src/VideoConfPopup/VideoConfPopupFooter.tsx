import { Box } from '@rocket.chat/fuselage';
import type { ComponentProps } from 'react';

const VideoConfPopupFooter = (props: ComponentProps<typeof Box>) => <Box mbs={28} {...props} />;

export default VideoConfPopupFooter;
