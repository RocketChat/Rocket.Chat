import type { BoxProps } from '@rocket.chat/fuselage';
import { Box } from '@rocket.chat/fuselage';

export type VideoConfPopupFooterProps = BoxProps;

const VideoConfPopupFooter = (props: VideoConfPopupFooterProps) => <Box marginBlockStart={28} {...props} />;

export default VideoConfPopupFooter;
