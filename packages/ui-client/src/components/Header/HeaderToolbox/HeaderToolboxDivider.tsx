import { Divider } from '@rocket.chat/fuselage';
import type { ReactElement } from 'react';

const HeaderToolboxDivider = (): ReactElement => <Divider mi={4} vertical />;

export default HeaderToolboxDivider;
