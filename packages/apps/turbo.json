{
    "extends": ["//"],
    "tasks": {
        "build": {
            "dependsOn": ["^build"],
            "outputs": ["base-runtime/**", "node-runtime/**", "scripts/**", "dist/**"]
        }
    }
}
