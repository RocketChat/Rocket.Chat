// Copyright 2018-2026 the Deno authors. MIT license.
// This module is browser compatible.

import { concat } from "jsr:@std/bytes@^1.0.6/concat";
import { DEFAULT_CHUNK_SIZE } from "./_constants.ts";
import type { Reader, ReaderSync } from "./types.ts";

/**
 * Read {@linkcode Reader} `r` until EOF (`null`) and resolve to the content as
 * {@linkcode Uint8Array}.
 *
 * @example Usage
 * ```ts ignore
 * import { readAll } from "@std/io/read-all";
 *
 * // Example from stdin
 * const stdinContent = await readAll(Deno.stdin);
 *
 * // Example from file
 * using file = await Deno.open("my_file.txt", {read: true});
 * const myFileContent = await readAll(file);
 * ```
 *
 * @param reader The reader to read from
 * @returns The content as Uint8Array
 */
export async function readAll(reader: Reader): Promise<Uint8Array> {
  const chunks: Uint8Array[] = [];
  while (true) {
    let chunk = new Uint8Array(DEFAULT_CHUNK_SIZE);
    const n = await reader.read(chunk);
    if (n === null) {
      break;
    }
    if (n < DEFAULT_CHUNK_SIZE) {
      chunk = chunk.subarray(0, n);
    }
    chunks.push(chunk);
  }
  return concat(chunks);
}

/**
 * Synchronously reads {@linkcode ReaderSync} `r` until EOF (`null`) and returns
 * the content as {@linkcode Uint8Array}.
 *
 * @example Usage
 * ```ts ignore
 * import { readAllSync } from "@std/io/read-all";
 *
 * // Example from stdin
 * const stdinContent = readAllSync(Deno.stdin);
 *
 * // Example from file
 * using file = Deno.openSync("my_file.txt", {read: true});
 * const myFileContent = readAllSync(file);
 * ```
 *
 * @param reader The reader to read from
 * @returns The content as Uint8Array
 */
export function readAllSync(reader: ReaderSync): Uint8Array {
  const chunks: Uint8Array[] = [];
  while (true) {
    let chunk = new Uint8Array(DEFAULT_CHUNK_SIZE);
    const n = reader.readSync(chunk);
    if (n === null) {
      break;
    }
    if (n < DEFAULT_CHUNK_SIZE) {
      chunk = chunk.subarray(0, n);
    }
    chunks.push(chunk);
  }
  return concat(chunks);
}

// denoCacheMetadata={"headers":{"accept-ranges":"bytes","cf-ray":"a3c223c8eeb7f254-GRU","content-length":"2037","x-robots-tag":"noindex","vary":"Origin","set-cookie":"__cf_bm=5UyHc3rCYTVFkbltubQziPWaOa9QcSPMYG60q3paHms-1789585496.4706798-1.0.1.1-Qyen5.nkG..sVSz5E2ojZEbUOwvXDstJruWlSMnhycjm9TtWI0K9dfwT8oidU4lbFDgKdQVSmcgIhlXHNN8Gz_IysrMWOB3_hisn53xa3Pq_eGz0vto7ThRgJlZw5Yqn; HttpOnly; SameSite=None; Secure; Path=/; Domain=jsr.io; Expires=Wed, 16 Sep 2026 19:34:56 GMT","cache-control":"public, max-age=31536000, immutable","age":"1736855","report-to":"{\"group\":\"cf-nel\",\"max_age\":604800,\"endpoints\":[{\"url\":\"https://a.nel.cloudflare.com/report/v4?s=mcrvTD7%2BIIBrYJk%2BUJk8sQsHJWQ89I0ABMBCww4OmQXwSYTiXBmsTDD%2FI8I%2BxwW00V0g25oillDm9RNPBYGhBovaifMYPG0DtD2lmnw22pn92LGPG6wNrcM%3D\"}]}","server":"cloudflare","access-control-allow-origin":"*","etag":"\"27249c8f6df85ff5ff4a8a29c6538aab\"","x-content-type-options":"nosniff","cf-cache-status":"HIT","alt-svc":"h3=\":443\"; ma=86400","access-control-expose-headers":"*","x-jsr-backend":"modules","nel":"{\"report_to\":\"cf-nel\",\"success_fraction\":0.0,\"max_age\":604800}","date":"Wed, 16 Sep 2026 19:04:56 GMT","content-type":"text/typescript","content-security-policy":"default-src 'none'; script-src 'none'; style-src 'none'; img-src 'none'; font-src 'none'; connect-src 'none'; frame-src 'none'; object-src 'none'; frame-ancestors 'none'; sandbox; form-action 'none';","cross-origin-resource-policy":"cross-origin"},"url":"https://jsr.io/@std/io/0.225.3/read_all.ts","time":1789585496}