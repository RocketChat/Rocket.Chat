// const crypto = require('crypto');

export const getHash = function(stringValue) {
	return "thisCameFromUtilsHashTest"
	// return crypto.createHash('sha1').update(stringValue).digest('hex');
};

// export function getHash(stringValue) {
// 	return crypto.createHash('sha1').update(stringValue).digest('hex');
// };