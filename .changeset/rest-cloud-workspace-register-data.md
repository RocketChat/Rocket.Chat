---
'@rocket.chat/rest-typings': minor
'@rocket.chat/meteor': minor
---

Adds a new `GET /v1/cloud.workspaceRegisterData` endpoint returning the base64 encoded workspace registration payload used by the offline cloud registration flow. It requires the `manage-cloud` permission.
