---
"@rocket.chat/rest-typings": minor
"@rocket.chat/meteor": minor
---

Adds `customFields[key]=value` and `includeCustomFields` parameters to the `users.list` endpoint, filtering users by exact custom field values and returning the fields the caller may read
