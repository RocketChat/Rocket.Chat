---
'@rocket.chat/rest-typings': minor
'@rocket.chat/meteor': minor
---

Adds a new `GET /v1/setupWizard.parameters` endpoint returning the settings flagged for the setup wizard along with a `serverAlreadyRegistered` flag.
