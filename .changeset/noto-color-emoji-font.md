---
'@rocket.chat/meteor': minor
---

Bundles the Noto Color Emoji font and applies it to all native emojis, so emojis render consistently across every platform (Windows, macOS, Linux) instead of falling back to each OS's built-in emoji set. The font is self-hosted and split by unicode-range, so only the emoji ranges actually displayed are downloaded.
