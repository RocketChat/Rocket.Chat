---
"@rocket.chat/meteor": patch
---

Require two-factor authentication for `users.create` and `users.createToken` REST API endpoints to prevent unauthenticated user creation via personal access tokens.
