import React, { FC, memo } from 'react';

// import { useCannedResponseForm } from '../../../../hooks/useCannedResponseForm';
// import CreateCannedResponseModal from './CreateCannedResponseModal';

const WrapCreateCannedResponseModal: FC = () => (
	// const form = useCannedResponseForm();

	// return <CreateCannedResponseModal form={form} onSave={onSave}/>;
	<p>save</p>
);
export default memo(WrapCreateCannedResponseModal);
