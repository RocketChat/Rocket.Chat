import React, { FC } from 'react';

import NotificationStatus from './NotificationStatus';

const All: FC = (props) => <NotificationStatus label='mention-all' bg='#F38C39' {...props} />;

export default All;
