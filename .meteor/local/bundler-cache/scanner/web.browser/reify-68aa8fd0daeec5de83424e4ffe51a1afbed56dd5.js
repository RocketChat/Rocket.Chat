let createReduce;module.link('./_createReduce.js',{default(v){createReduce=v}},0);

// **Reduce** builds up a single result from a list of values, aka `inject`,
// or `foldl`.
module.exportDefault(createReduce(1));
