module.export({IncomingRequestMessage:()=>IncomingRequestMessage});let IncomingMessage;module.link("./incoming-message",{IncomingMessage(v){IncomingMessage=v}},0);
/**
 * Incoming request message.
 * @public
 */
class IncomingRequestMessage extends IncomingMessage {
    constructor() {
        super();
    }
}
