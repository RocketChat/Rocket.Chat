let createPredicateIndexFinder;module.link('./_createPredicateIndexFinder.js',{default(v){createPredicateIndexFinder=v}},0);

// Returns the last index on an array-like that passes a truth test.
module.exportDefault(createPredicateIndexFinder(-1));
