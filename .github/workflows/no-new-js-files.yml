name: "JS file preventer"
on:
  pull_request:
    types: [opened, edited]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: ggazzo/no-js-action@1.0.0
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
